<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smartclassroom">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/smartlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/smartlogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AboutAdmin"
            android:label="@string/title_activity_about_admin"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".MyProfile"
            android:label="@string/title_activity_building"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ControlPanel"
            android:label="@string/title_activity_control_panel"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Room" />
        <activity android:name=".Login" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>